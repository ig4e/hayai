package exh.ui.intercept

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Activity that handles deep links for EH/EXH, NHentai, MangaDex,
 * Pururin, HBrowse, Tsumino gallery URLs.
 *
 * TODO: Full implementation will be provided by the EH UI agent.
 */
class InterceptActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Will be implemented by the EH UI agent
        finish()
    }
}
