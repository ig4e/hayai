package hayai.novel.source

/**
 * Marker interface for sources that provide text/HTML content instead of images.
 * Used by the reader to select the text viewer instead of the image viewer.
 */
interface TextSource
